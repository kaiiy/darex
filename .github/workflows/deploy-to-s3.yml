on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-22.10
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: yarn

      - run: yarn install
      - run: yarn build
